# Functional Delta: YU02-lmax-kubernetes

- inherit the functional surface of `014-fdc3-intent-interoperability`
- replace inherited trading-path internals with the `009b` LMAX architecture
- keep inherited FDC3 intent/context flows unless explicitly changed
