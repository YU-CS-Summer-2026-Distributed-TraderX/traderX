{
  "title": "YU17-otc-rates architecture",
  "stateId": "YU17-otc-rates",
  "parentState": "YU16-cdm-instruments",
  "description": "OTC interest-rate swaps and swaptions on the cluster tier: the first instrument class that neither matches nor nets. POST /swaps and POST /swaptions validate every term the fixed 64-byte record cannot represent and offer one sequenced command; the clustered service applies it into a replicated contract store and never hands it to the matching engine, because an OTC contract has no book to rest in and no position to accumulate. The risk gate gets a swap path - the notional is the notional, and quantity x rate would understate a 10mm swap by 24x. One EOD cut at one consensus sequence carries two sections and renders two artifacts: the netted position extract unchanged at CSV schema 3, and a per-contract OTC artifact carrying terms and no valuation, with swaps and swaptions sharing every column that describes the underlying.",
  "inheritsFrom": "YU16-cdm-instruments",
  "flowDoc": "architecture.md",
  "mermaidDirection": "LR",
  "nodes": [
    {
      "id": "swap_client",
      "kind": "external",
      "label": "rates desk (RFQ, bilateral)",
      "description": "Swaps trade OTC, bilaterally, by request for quote. There is no order flow and no book to post into \u2014 a booking is the confirmation of a trade already agreed away from the venue, which is why nothing in this state matches."
    },
    {
      "id": "gateway",
      "kind": "service",
      "label": "cluster gateway (POST /swaps, /swaptions)",
      "description": "One shared validator behind two routes, so they cannot drift into disagreeing about what a notional or a date is. Refuses, before anything is sequenced, every term the record cannot represent: a notional past int range (asLong, never asInt, because asInt saturates and would admit 5bn as 2.147bn), a date past 2149-06-06 (the packed fields mask and would wrap into a plausible date), a maturity at or before the effective date, a zero rate, an unknown conventions or exercise-style name, and a swaption expiry after the underlying's effective date. 400 means never sequenced; 422 means sequenced and refused by the gate."
    },
    {
      "id": "conventions",
      "kind": "store",
      "label": "SwapConventions (compiled-in tables)",
      "description": "Two index-addressed tables stored nowhere in replicated state: five market conventions (float index, payment frequency, day count, currency) and three exercise styles (European, Bermudan, American). The pattern the option multiplier and the bond book grid already use. Append only: an index that has been journaled keeps its meaning forever, and an unknown index aborts the render rather than resolving to another - a Bermudan published as European is a different instrument."
    },
    {
      "id": "consensus_log",
      "kind": "queue",
      "label": "Aeron consensus log (types 12 and 13 on template 1)",
      "description": "Both bookings are ordinary committed log entries. The codec copies commandType through without interpreting it, so two new command types cost no schema change. The product is the COMMAND, never a field's value: deriving 'this is a swaption' from a non-zero expiry would make 1970-01-01 a load-bearing sentinel. Sequencing is what keeps deterministic replay, byte-identical rendering on all three members, the quiescence witness and reproducibility from the stored cut."
    },
    {
      "id": "risk_gate",
      "kind": "service",
      "label": "BlpRiskState.decideSwapBooking",
      "description": "The same ordered pipeline and stable precedence, measuring the NOTIONAL rather than quantity x price x multiplier, which would value a 10mm swap at 420,000. Drops the four checks that read state a swap does not have: security enabled/restricted/priced, and the position and concentration limits \u2014 the latter two because they project the very netting grain this state exists to show the loss of."
    },
    {
      "id": "contract_store",
      "kind": "store",
      "label": "contract store (replicated, T_CONTRACT, format 6)",
      "description": "Terms plus booking account, in booking order, capped at 4096 and refusing at capacity deterministically. contractId is the booking's own consensus sequence: unique within the epoch by construction, needing no generator, no snapshot header field and no restore invariant. A swaption's record is a swap's plus three columns, because its underlying IS a swap - and restore reads a record at the width its FORMAT declares, since onSnapshotRecord is handed no length and a format-5 record read at the new width would take the next record's bytes as an expiry."
    },
    {
      "id": "engine",
      "kind": "service",
      "label": "matching engine (untouched)",
      "description": "Byte-identical to the YU16 layer. A swap never reaches it \u2014 no order, no book entry, no crossing, no trade, no position \u2014 which is what makes 'a swap changes nothing for the instruments that already work' structural rather than asserted, and leaves the order hot path exactly what the allocation gates already measure."
    },
    {
      "id": "cut",
      "kind": "store",
      "label": "the cut at N (schema 3, two sections)",
      "description": "One rendered artifact at one consensus sequence carrying the netted positions and then, after a #contracts marker, the OTC contracts. A section rather than a second message: two messages could be delivered apart, hashed apart and stored apart, which is the consistent-at-two-instants failure a sequenced cut exists to rule out. The section is emitted even when empty, because an absent section means an older producer."
    },
    {
      "id": "risk_extract",
      "kind": "service",
      "label": "risk-extract (two artifacts, one stamp)",
      "description": "Renders both files from the same cut under the same stamp, so they share consensusSequence, sessionDate and cutSha256 by construction. Writes both write-once beside the single stored cut and announces both hashes on risk.extract.ready. --rebuild reproduces both from that cut alone."
    },
    {
      "id": "netted_csv",
      "kind": "store",
      "label": "seq-N.csv (CSV schema 3, unchanged)",
      "description": "Equities, ETFs, Treasuries and listed options at the (accountId, security) grain, where netting is exact. Every column keeps its name, position and meaning; the reader stops at the section marker, so no swap row can appear here."
    },
    {
      "id": "contracts_csv",
      "kind": "store",
      "label": "seq-N-contracts.csv (schema 2, terms only)",
      "description": "One row per booked contract, swaps and swaptions together: direction, notional, rate or strike, both underlying dates, float index, frequency, day count, currency, counterparty, netting set, then productType, expiry and exercise style. A swap leaves the last two empty. No NPV, no mark, no curve, no sensitivity - the consumer's engine is authoritative for what a contract is worth, and a second differently-derived number here would be a reconciliation break rather than data."
    },
    {
      "id": "consumer",
      "kind": "external",
      "label": "pricing / risk engine",
      "description": "Reads both artifacts as one portfolio taken at one instant. Receives the terms it needs to value each contract and does the valuation itself; the netted file it already consumes is unchanged, so it reads without a code change."
    }
  ],
  "edges": [
    {
      "from": "swap_client",
      "to": "gateway",
      "label": "POST /swaps, /swaptions (agreed bilaterally, never matched)"
    },
    {
      "from": "conventions",
      "to": "gateway",
      "label": "name -> index, or 400 pre-consensus"
    },
    {
      "from": "gateway",
      "to": "consensus_log",
      "label": "TYPE_SWAP_BOOK / TYPE_SWAPTION_BOOK on template 1 (no new schema)"
    },
    {
      "from": "consensus_log",
      "to": "risk_gate",
      "label": "applied on every member, in log order"
    },
    {
      "from": "risk_gate",
      "to": "contract_store",
      "label": "accepted: terms stored, notional accrued to credit"
    },
    {
      "from": "risk_gate",
      "to": "gateway",
      "label": "refused: KIND_SWAP_BOOKED with the RiskReason (422)"
    },
    {
      "from": "consensus_log",
      "to": "engine",
      "label": "every OTHER command, unchanged"
    },
    {
      "from": "contract_store",
      "to": "cut",
      "label": "#contracts section at sequence N"
    },
    {
      "from": "engine",
      "to": "cut",
      "label": "netted positions at the same N"
    },
    {
      "from": "cut",
      "to": "risk_extract",
      "label": "one cut, one cutSha256"
    },
    {
      "from": "conventions",
      "to": "risk_extract",
      "label": "index -> float index, frequency, day count, exercise style"
    },
    {
      "from": "risk_extract",
      "to": "netted_csv",
      "label": "rendered, write-once"
    },
    {
      "from": "risk_extract",
      "to": "contracts_csv",
      "label": "rendered from the same bytes, write-once"
    },
    {
      "from": "netted_csv",
      "to": "consumer",
      "label": "unchanged schema 3"
    },
    {
      "from": "contracts_csv",
      "to": "consumer",
      "label": "terms; valuation is theirs"
    }
  ]
}
