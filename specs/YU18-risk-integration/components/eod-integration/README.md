# EOD integration component

Status: implemented foundations; see the preserved task list for remaining work.

[Specification](spec.md), [plan](plan.md), [tasks](tasks.md). Existing FR-EB/NFR-EB/SC-EB IDs remain stable. Runtime source stays at `../../generation/runtime-overrides/eod-risk-bundles/`; the runtime directory and wire schema names are not renamed. Shared schemas, architecture and quickstart remain in the state parent.

The spec was moved from the state root on 2026-09-23 without changing its financial requirements. Older scope/status prose is historical and must be reconciled against current evidence before extending the component.
