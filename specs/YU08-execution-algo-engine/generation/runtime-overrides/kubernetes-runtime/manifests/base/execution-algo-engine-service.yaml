apiVersion: v1
kind: Service
metadata:
  name: execution-algo-engine
  namespace: traderx
  labels:
    app: execution-algo-engine
spec:
  type: ClusterIP
  selector:
    app: execution-algo-engine
  ports:
    - name: http
      protocol: TCP
      port: 18120
      targetPort: 18120
